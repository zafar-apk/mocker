# Repository Instructions

- The Mocker app is expected to run on port 3000.
- After making edits, use the existing `http://localhost:3000` server for verification instead of starting another instance on a new port.
- Only start a new server or use another port if the user explicitly asks for it, or if port 3000 is unavailable and verification cannot proceed otherwise.
